rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas de seguridad para la colección de clientes (ADR-016)
    match /customers/{document} {
      // Solo usuarios autenticados mediante Firebase Auth pueden leer y escribir
      allow read, write: if request.auth != null;
    }
  }
}
